# ============================================================
# 极简记账本 Pro + 数据处理可视化看板 - 依赖配置
# ============================================================

# ---- 静态前端 (index.html) ----
# 无需安装任何依赖，双击 index.html 即可在浏览器中运行
# 前端通过 CDN 引入：Tailwind CSS、Chart.js、CryptoJS

# ---- Streamlit 数据处理可视化看板 ----
# 启动命令：streamlit run app.py
streamlit>=1.28.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
plotly>=5.15.0,<6.0.0
openpyxl>=3.1.0

# 可选：图表导出为图片
# kaleido>=0.2.1